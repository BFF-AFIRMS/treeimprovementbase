<%once>
use utf8;
</%once>

<%perl>
  my $path = $c->_env->{PATH_INFO};
</%perl>


% if ( $path =~ m"^/(svelte|nuxt)/") {

% $m->call_next;

% } else {
  <& /site/header.mas &>
% $m->call_next;
  <& /site/footer.mas &>
% }
