// prerendering is required to build a static site
export const prerender = true;
// disable Client-Side-Rending so that each page load will
// properly go through the perl backend
//export const csr = false;
//export const ssr = true;
